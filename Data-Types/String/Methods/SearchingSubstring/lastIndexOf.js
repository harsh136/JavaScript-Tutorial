// just like indexOf() lastIndexOf() also searches for substring but it search from the last character of the string
// it search from laft to right, unlike right to left like indexOf()

let text = "harsh";

console.log(text.indexOf("h")); //0
console.log(text.lastIndexOf("h")); //4
