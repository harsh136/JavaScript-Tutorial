// they do exactly what they say
// startsWith() chech if a string starts with specific set of characters or not
const userName = "harsh";
console.log(userName.startsWith("har")); // true

// endsWith() chech if a string ends with specific set of characters or not
console.log(userName.endsWith("har")); // false
